---
global:
  smtp_smarthost: {{ am_smtp_smarthost }}
  smtp_from: {{ am_smtp_from }}
  smtp_require_tls: true
  smtp_auth_username: {{ am_smtp_auth_username }}
  smtp_auth_identity: {{ am_smtp_auth_username }}
  smtp_auth_password: {{ am_smtp_auth_password }}

route:
  receiver: email
  #group_by: ['alertname']
  #group_wait: 30s
  #group_interval: 5m
  #repeat_interval: 1h

receivers:
  - name: email
    email_configs:
    - to: {{ am_drio_email }}
      send_resolved: true
